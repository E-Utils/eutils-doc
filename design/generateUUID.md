## 生成唯一id
```
	函数名： generateUUID（UUID－通用唯一标识Universally Unique Identifier）
	入参：无；
	返回值：生成后的唯一uuid；
	简介：UUID是由一组32个16进制数字所构成，因此UUID理论上的总数为：3.4*10^38，
	也就是说若每纳秒产生1兆个uuid，需要花100亿年才能用完所有的uuid，所以它是唯一的，
	通过时间戳、随机数等来生成唯一的uuid
```